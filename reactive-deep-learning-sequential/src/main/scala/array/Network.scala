package array

object Network {
  def process(topology: Array[Array[Option[Double]]], layers: Array[Int], input: Array[Double]) = {

    /*for(
      i <- 0 to topology.length;
      val weights = topology.
    )
    Perceptron.output()*/

  }
}
